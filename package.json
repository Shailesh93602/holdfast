{
  "name": "holdfast",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Holdfast — an inventory reservation engine that never oversells under concurrency. Benchmarks pessimistic-lock vs optimistic-CAS vs atomic-conditional strategies against real Postgres.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "db:generate": "drizzle-kit generate",
    "migrate": "tsx src/infra/migrate.ts",
    "seed": "tsx src/infra/seed.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "bench": "tsx src/bench.ts",
    "bench:sharded": "tsx src/sharded-bench.ts",
    "typecheck": "tsc --noEmit",
    "loadtest": "k6 run loadtest/k6-reserve.js"
  },
  "dependencies": {
    "drizzle-kit": "^0.30.1",
    "drizzle-orm": "^0.38.3",
    "fastify": "^5.2.0",
    "pg": "^8.13.1",
    "prom-client": "^15.1.3",
    "tsx": "^4.19.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/pg": "^8.11.10",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
