# Postgres connection (defaults to a local DB named "quickcommerce")
DATABASE_URL=postgresql://localhost:5432/quickcommerce
# Max pool connections — set high enough that concurrency tests actually contend
PG_POOL_MAX=24
# HTTP server port
PORT=3000
